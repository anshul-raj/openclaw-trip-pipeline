# Trips Folder

This folder stores per-trip configuration for multi-tenant ingestion.

Each trip must have:

- `/memory/multi-trip-link-logger/trips/<tripname>/details.md`

Example:

- `/memory/multi-trip-link-logger/trips/example_trip/details.md`
